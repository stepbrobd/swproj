[project]
name = "swproj"
version = "2026.518.0"
description = "toolkit for sonarworks soundid"
readme = "readme.md"
requires-python = ">=3.14"
dependencies = ["click", "matplotlib", "numpy", "pyyaml"]

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"

[project.scripts]
swproj = "swproj.cli:cli"
